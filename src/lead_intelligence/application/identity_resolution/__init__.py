"""The Identity Resolution Engine.

Public entry points: IdentityResolutionEngine (engine.py),
IdentityResolutionProfile / default_profile (config.py), and
recompute_review_item (recomputation.py). See README.md in this folder for
how the pieces fit together, and the approved Identity Resolution RFC for
*why* each piece exists.
"""
